apiVersion: v1
kind: Service
metadata:
  name: cart-service
  namespace: shopflow
  labels:
    app: cart-service
spec:
  selector:
    app: cart-service
  ports:
    - port: 8083
      targetPort: 8083
  type: ClusterIP
